#include <stdio.h>
#include <stdlib.h>
#include "lists.h"
/**
 * check_cycle - check if the linked list is cycle
 * @head: pointer to the list that can be checked
 * Return: Return integer
 */
int check_cyle(listint_t *list)
{
    if (list == NULL)
        return (0);
    return (1);
}
